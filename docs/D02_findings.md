# D02 衛生類表格清單盤點（2026-09-09）— 部分受阻

## 嘗試與結果
| 路徑 | 結果 |
|---|---|
| `query.php?Action_From=level&lvbw=C7`（主題類：衛生） | ❌ 回首頁 5,633b |
| `query.php?Action_From=level&lvbw=BB07`（衛生統計書） | ⚠️ 回 693,250b，但**內含 0 個可用連結** |
| `query.php` POST 基本檢索（癩／衛生／死因／就學） | ❌ 皆回首頁 5,633b |
| 帶 session cookie（TS01fe5a75）重試 | ❌ 無改善 |

## 診斷
- `main_browse.php` 的靜態樹**可完整抽取**（已得 1,325 個 lvbw 代碼）
- 但**展開後的頁面不含 href**，樹狀展開與結果清單推測由 JS 於前端處理，
  或需 `Access_Num` 這個由前端產生的 token
- 這是 2008 年建置的系統，未提供任何機器可讀介面

## 目前能拿到 / 拿不到
✅ 完整分類體系與 1,325 個節點代碼（含 BB07 衛生統計書、BB17 戶口調查、C7 衛生、C1 教育）
❌ 表格層級的 103,732 筆詮釋資料（表名、年份、頁碼）
❌ 統計表影像的直接 URL

## 對專案的影響（重要，但不必然致命）
**要做的事其實不依賴自動化索引。**
專案需要的是：找到衛生類統計表 → 取得影像 → 人工轉錄數值。
人在瀏覽器裡可以完成前兩步；失去的只是「自動建立索引」的能力。

但這確實改變了工作量估計：
- 原設想：自動抓表格清單 → 快速定位目標序列
- 實際：需人工瀏覽定位，再逐表處理

## 建議的下一步（跳出反向工程）
1. **用瀏覽器實際操作一次**該站（人工 2 分鐘可完成，勝過繼續猜 API）
   —— 本 session 有 Chrome 自動化工具可用，但需使用者授權該網站權限
2. 若人工可順利取得影像，改以「單一統計序列」為單位手動蒐集，放棄全庫索引
3. 並行查核 D11「台灣統計50年」（中研院資訊所）是否已提供數值化資料
   —— 若有，可能直接省下大量轉錄工作

## 狀態
標為完成（已釐清可行與不可行的邊界），但**表格清單未取得**，
實際盤點需改用瀏覽器路徑或改採 D11 的替代來源。
